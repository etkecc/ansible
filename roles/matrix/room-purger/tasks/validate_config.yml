---
- name: Fail if required settings not defined
  fail:
    msg: >-
      You need to define a required configuration setting (`{{ item }}`).
  when: "vars[item] == ''"
  with_items:
    - "matrix_room_purger_login"
    - "matrix_room_purger_password"
    - "matrix_room_purger_search"
