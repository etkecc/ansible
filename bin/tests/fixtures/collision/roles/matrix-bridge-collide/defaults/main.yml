# Project source code URL: https://github.com/example/collide-b
# bridge_collide_version derives to the same "Collide" as matrix_collide_version
# in the sibling role: the collision that must make the diff assert scream.
bridge_collide_version: "2.0.0"
